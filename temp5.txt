rdPanel() {
      const panel = document.getElementById('pageDescPanel');
      if (panel) {
          panel.classList.toggle('active');
          const btn = document.querySelector('.prd-toggle');
          btn.innerText = panel.classList.contains('active') ? "PRD DESCRIPTION: ON" : "PRD DESCRIPTION: OFF";
      } else {
          // If the user's custom logic hasn't loaded yet
          console.log("Description Module DB Not Connected yet.");
      }
  }

  // Demo Toggle for Quest 1
  window.setDemoState = function(state) {
    const loginOverlay = document.getElementById('loginOverlay');
    const completeOverlay = document.getElementById('completeOverlay');
    const refInput = document.getElementById('refInput');
    const agreeCheck = document.getElementById('agreeCheck');
    const preRegBtn = document.getElementById('preRegBtn');
    
    if (state === 0) {
      loginOverlay.style.display = "flex";
      completeOverlay.style.display = "none";
      refInput.disabled = true;
      agreeCheck.disabled = true;
      preRegBtn.disabled = true;
      preRegBtn.style.opacity = "0.5";
    } else if (state === 1) {
      loginOverlay.style.display = "none";
      completeOverlay.style.display = "none";
      refInput.disabled = false;
      agreeCheck.disabled = false;
      preRegBtn.disabled = false;
      preRegBtn.style.opacity = "1";
    } else {
      loginOverlay.style.display = "none";
      completeOverlay.style.display = "flex";
      refInput.disabled = true;
      agreeCheck.disabled = true;
      preRegBtn.disabled = true;
      preRegBtn.style.opacity = "0.5";
    }
  };
</script>

<!-- Description Module Container -->
<div class="page-desc-panel" id="pageDescPanel">
    <div class="pdp-header">
        <h2 style="font-size:16px; font-weight:900; color:#0f172a;">ROSEA  </h2>
        <button id="lockToggleBtn" onclick="toggleLock()" style="background:#0f172a; color:#fff; border:none; padding:6px 12px; border-radius:8px; font-size:12px; font-weight:800; cursor:pointer;">   </button>
    </div>
    <div class="pdp-body" id="descContent">
        <!-- DB / Description Custom Logic loaded here -->
    </div>
</div>

<!-- PRECAUTIONS MODAL -->
<div id="precautionsModal" style="display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.5); z-index: 9999; align-items: center; justify-content: center; backdrop-filter: blur(2px);">
  <div style="background: #fff; width: 500px; padding: 30px; border-radius: 8px; box-shadow: 0 10px 25px rgba(0,0,0,0.2); position: relative;">
    <button onclick="document.getElementById('precautionsModal').style.display='none'" style="position: absolute; top: 15px; right: 15px; background: none; border: none; font-size: 24px; cursor: pointer; color: #a0aec0;">&times;</button>
    <h3 id="precautionsModalTitle" style="margin-bottom: 15px; color: #2d3748; font-size: 18px; font-weight: 800;">Precautions</h3>
    <ul style="font-size: 13px; color: #4a5568; line-height: 1.6; font-weight: 500; margin-bottom: 0; padding-left: 20px;">
      <li style="margin-bottom: 8px;">Rewards will be distributed after the official launch.</li>
      <li style="margin-bottom: 8px;">Event details are subject to change without prior notice.</li>
      <li style="margin-bottom: 8px;">Users participating through illegitimate means will be excluded from rewards.</li>
    </ul>
  </div>
</div>

<!-- Q4 REWARDS MODAL -->
<div id="q4RewardsModal" style="display: none; positi